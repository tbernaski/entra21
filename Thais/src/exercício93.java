/*import java.util.Scanner;

public class exercício93 {
    public static void main(String[] args) {
        Scanner in = new Scanner(System.in);

        System.out.print("Informe uma posição para O: ");
        int lugarparaO = in.nextInt();

        System.out.print("Informe uma posição para X: ");
        int lugarparaX = in.nextInt();

        int jogador1 = in.nextInt();
        int jogador2 = in.nextInt();
        int numeros = in.nextInt();

        int[][] posição = new int[3][3];


        for (int i = 0; i < posição.lenght; i++) {
            for (int=0);

            if (posição.length() == 3) {
                switch (clazz) {
                    case "1", "2", "3", "4", "5","6","7", "8", "9":
                        System.out.println("Tank");
                        break;

                    default:
                        System.out.println("Posição Inválida! Por favor informe uma Posição Válida!");
                        isHealer = false;
                        break;
                }
            }
        }
    }
}
 */
